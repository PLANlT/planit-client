name: Slack Noticat
on:
  pull_request:
    types: [opened]

jobs:
  notify:
    name: Slack Noticat
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: getPrUrl
        id: pr-url
        uses: ammaratef45/pr-url-action@v1
        
      - name: Send to Slack
        uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: |
            {
              "text": "잘 지냈냐옹? 새로운 PR이 올라왔다옹!",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "잘 지냈냐옹? 새로운 PR이 올라왔다옹! 리뷰 달라옹!! <br> ${{ steps.pr-url.outputs.url }}"
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "<!이창민> <!전도연>"
                  }
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_CAT_WEBHOOK }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
